import {FEATURED_TAGS} from '../../Shared/Shared__types.js';

const featuredTags__reducer = (state = [], action) => {
  switch(action.type) {
    default:
      return state;
  };
};

export default featuredTags__reducer;